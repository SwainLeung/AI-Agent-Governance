# Runtime reports

Generated inventory, Dashboard state, adoption queues, evidence, and rollback archives are private runtime outputs. They are ignored by Git and should remain in the local workspace.

The public repository keeps only schemas, generators, policy documents, and sanitized examples. Populate `config/project-roots.local.json` locally before generating the private Dashboard.
